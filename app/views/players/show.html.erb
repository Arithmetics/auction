<h1><%= @player.player_name %></h1>
<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to "Details", [@player, game] %>
      <%= game.season %>,
      <%= game.week %>,
      <%= game.rushing_yards %>,
      <%= game.rushing_touchdowns %>
    </li>
  <% end %>
</ul>
